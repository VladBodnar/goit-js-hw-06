const listLiItemRef = document.querySelectorAll('.item');
console.log('Number of categories:',listLiItemRef.length);

const listLiCategoryRef = document.querySelectorAll('h2');
console.log(...listLiCategoryRef);

const listLiElementssRef = h2.querySelectorAll('li');
console.log(listLiElementssRef);
